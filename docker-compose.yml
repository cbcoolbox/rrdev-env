version: "3"
services:
  rr-app:
    image: "rr-app"
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./src:/app
      - composer:/app/vendor
# creating this volumn to overwrite the vendor folder
# the container will run composer install itself.
volumes:
  composer: